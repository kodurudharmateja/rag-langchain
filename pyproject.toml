[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kodurudharamteja", email = "kodurudharmateja23@gmail.com" }
]
requires-python = ">=3.8"
dependencies = [
    "ipykernel>=6.29.5",
]

[project.scripts]
rag = "rag:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
